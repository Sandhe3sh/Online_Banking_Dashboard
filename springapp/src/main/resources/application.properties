# spring.datasource.url=jdbc:mysql://localhost:3306/onlinebankingdb?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=root
# spring.jpa.hibernate.ddl-auto=update
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# # Database
# spring.datasource.url=jdbc:mysql://localhost:3306/onlinebankingdb?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=root
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # Hibernate / JPA
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# # spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect  ← remove or update if needed

# # Web Server (change port to avoid 8080 conflict)
# server.port=8081
# Server port (change if 8080 is busy)
server.port=8081

# MySQL connection (adjust user/pass/db as needed)
#spring.datasource.url=jdbc:mysql://localhost:3306/onlinebankingdb?useSSL=false&serverTimezone=UTC
spring.datasource.url=jdbc:mysql://localhost:3306/OnlineBankingDB
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA & Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
